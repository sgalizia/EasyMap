# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'EasyMap' do
  use_frameworks!

  # Pods for EasyMap

  target 'EasyMapTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Quick'
    pod 'Nimble'
  end

end
